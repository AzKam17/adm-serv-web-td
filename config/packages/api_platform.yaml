api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]

    formats:
        json: [ 'application/json' ]
        xml: [ 'application/xml', 'text/xml' ]
        html: [ 'text/html' ]

    title: 'MAGIR Library API'

    description: 'Démo API Rest'

    version: '10.04.21'
